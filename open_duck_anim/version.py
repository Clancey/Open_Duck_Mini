"""Package version.

Single source of truth for the ``open_duck_anim`` version string. The compiler
stamps ``compiler_version`` into ``.duckanim`` provenance using this value
(plan §5.2, §4.1-C).
"""

__version__ = "0.1.0"
